
{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a customer's personal information.",
      "properties": {
        "id": { "type": "string" },
        "username": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "registrationDate": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "username", "email", "registrationDate"]
    },
    "Product": {
      "title": "Product",
      "type": "object",
      "description": "An individual item in the catalog.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "sku": { "type": "string" },
        "price": { "type": "number", "description": "The actual offer price the retailer pays." },
        "mrp": { "type": "number", "description": "The maximum retail price for comparison." },
        "costPrice": { "type": "number", "description": "The internal acquisition cost/rate value for the admin." },
        "stockQuantity": { "type": "number" },
        "category": { "type": "string" },
        "distributorName": { "type": "string", "description": "The name of the distributor who supplied the stock." },
        "imageUrl": { "type": "string", "description": "Primary public URL for the product identity image." },
        "imageUrls": { "type": "array", "items": { "type": "string" }, "description": "Up to 3 product identity images for multi-angle view." }
      },
      "required": ["id", "name", "price", "stockQuantity"]
    },
    "Order": {
      "title": "Order",
      "type": "object",
      "description": "A stock request or damage return from a retailer.",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "productId": { "type": "string", "description": "Primary SKU ID for single-item logs or returns." },
        "items": { "type": "string" },
        "quantity": { "type": "number" },
        "total": { "type": "number" },
        "profit": { "type": "number", "description": "Net profit generated from this order (Revenue - Cost)." },
        "status": { "type": "string", "enum": ["pending", "processing", "shipped", "delivered", "cancelled", "return_pending", "returned"] },
        "paymentMethod": { "type": "string", "enum": ["cash", "after_delivery", "paid"] },
        "storeName": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phoneNumber": { "type": "string" },
        "deliveryAddress": { "type": "string" },
        "location": { "type": "string", "description": "Regional grid coordinate string." },
        "damageImageUrl": { "type": "string", "description": "Visual proof signature for damage audits." },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "userId", "total", "status", "phoneNumber", "deliveryAddress", "paymentMethod"]
    },
    "Store": {
      "title": "Store",
      "type": "object",
      "description": "A retailer's branch details.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "managerName": { "type": "string" },
        "location": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phoneNumber": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "active", "rejected"] },
        "imageUrl": { "type": "string", "description": "Retailer profile or branch photo identity." }
      },
      "required": ["id", "name", "status"]
    },
    "AdminRole": {
      "title": "AdminRole",
      "type": "object",
      "description": "Administrative authority record.",
      "properties": {
        "uid": { "type": "string" },
        "role": { "type": "string", "enum": ["master", "regional"] }
      },
      "required": ["uid", "role"]
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "indexes": [
      {
        "collectionGroup": "orders",
        "queryScope": "COLLECTION",
        "fields": [
          { "fieldPath": "userId", "order": "ASCENDING" },
          { "fieldPath": "createdAt", "order": "DESCENDING" }
        ]
      }
    ],
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "User profiles."
    },
    "/products/{productId}": {
      "schema": "Product",
      "description": "Global product catalog."
    },
    "/orders/{orderId}": {
      "schema": "Order",
      "description": "Consolidated store orders and damage reports."
    },
    "/stores/{storeId}": {
      "schema": "Store",
      "description": "Retailer registration records."
    },
    "/roles_admin/{uid}": {
      "schema": "AdminRole",
      "description": "Registry of authorized administrative identities."
    }
  }
}
